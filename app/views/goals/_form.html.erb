<% goal_record = local_assigns[:goal] %>

<% if goal_record %>
  <%= form_with model: goal_record, url: goal_path(goal_record), method: :patch, class: "goal-map-form", data: { turbo_frame: "goal_form" } do |form| %>
    <% if goal_record.errors.any? %>
      <div class="goal-map-form__errors">
        <% goal_record.errors.full_messages.each do |message| %>
          <p><%= message %></p>
        <% end %>
      </div>
    <% end %>

    <%= form.label :title, "目標" %>
    <%= form.text_field :title, placeholder: "例: 3か月後に小さなRailsアプリを公開する", autocomplete: "off" %>

    <%= form.label :description, "目標の説明" %>
    <%= form.text_field :description, placeholder: "例: どんな状態になればゴール達成か", autocomplete: "off" %>

    <%= form.label :value_statement, "価値観" %>
    <%= form.text_area :value_statement, rows: 3, placeholder: "例: なぜこのゴールを目指したいか、大切にしたいこと", autocomplete: "off" %>

    <%= form.submit "目標と価値観を更新" %>
  <% end %>
<% else %>
  <form class="goal-map-form">
    <label>目標</label>
    <input type="text" placeholder="例: 3か月後に小さなRailsアプリを公開する">
    <label>目標の説明</label>
    <input type="text" placeholder="例: どんな状態になればゴール達成か">
    <label>価値観</label>
    <textarea rows="3" placeholder="例: なぜこのゴールを目指したいか、大切にしたいこと"></textarea>
    <button type="button">目標と価値観を更新</button>
  </form>
<% end %>
